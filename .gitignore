# dependencies
node_modules

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem
*.log

# env files
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# sandbox infra (not part of the site)
.zscripts/
Caddyfile
agent-ctx/
dev.log
server.log

# skills + audit working data (not site source)
/skills/
/audit_data/
/tool-results/
/download/
/scripts/*
# the image pipeline is part of the site's workflow — keep it tracked
!/scripts/generate_images.py
/scripts/.raw/
/worklog.md

# single lockfile policy (bun.lock is canonical)
package-lock.json

# local Claude Code MCP config — real key lives here, never in the repo
.mcp.json
